## Disk space

The script checks usage of each partition in the system. When any of the disk space used is above 80%, the script prints a WARNING message.

Otherwise, it prints a OK message with usage percentage.
